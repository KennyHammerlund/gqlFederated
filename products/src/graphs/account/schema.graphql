
scalar Date
scalar DateTime

type Query {
  account(input: AccountInput!): Account
}

type Account {
  id: ID!
  displayName: String!
  token: String!
}

input AccountInput {
  displayName: String!
}
